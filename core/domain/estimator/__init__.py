"""The project estimator: per-service question sets and the pricing engine
that turns a customer's answers into a price/duration range. Pure Python,
no Django/aiogram — the presentation layer (apps.bot) drives the actual
conversation and calls into this.
"""
